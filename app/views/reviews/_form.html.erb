<%= simple_form_for [@booking, @review] do |f| %>
  <%= f.input :rating_user, collection: [1, 2, 3, 4, 5] %>
  <%= f.input :picture %>
  <%= f.input :picture_cache, as: :hidden %>
  <%= f.button :submit, "Add Review", class: "btn btn-success"%>
<% end %>
