<%= simple_form_for [@booking, @review] do |f| %>
  <%= f.input :rating, collection: [1, 2, 3, 4, 5] %>
  <%= f.input :content %>
  <%= f.input :picture %>
  <%= f.input :picture_cache, as: :hidden %>
  <%= f.button :submit, "Add Review", class: "btn-meli-square-small"%>
<% end %>
