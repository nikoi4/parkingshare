<% sender = chat.user == current_user ? "me" : "him" %>
​
<div class="chat-bubble-wrapper">
  <div class="chat-bubble <%= sender %>">
    <div class="chat-username"><%= chat.user.identifierfull %></div>
    <div class="chat-message"><%= chat.message %></div>
  </div>
</div>
